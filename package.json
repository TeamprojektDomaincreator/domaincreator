{
  "name": "dxf-playground",
  "version": "1.0.0",
  "description": "Simple program to test out loading a dxf file",
  "main": "docs/index.html",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outdir=docs",
    "serve": "esbuild src/main.ts --bundle --outdir=docs --servedir=docs --serve --watch"
  },
  "author": "Timothy Hale",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.20.2",
    "typescript": "^5.4.3"
  },
  "dependencies": {
  }
}
